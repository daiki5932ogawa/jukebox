<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
    <script src="video.js"></script>
        <ul id="navigation">
            動画再生画面
        </ul>
        <div id="video"/div>
        {#ビデオ一覧のループを書く#}
        <iframe width="420" height="315" src="{{ single_video.getUrl }}" frameborder="0" allowfullscreen></iframe><br>
        <a>再生中の動画INFO</a><br><br>
        <a>ビデオURL: </a><a href="{{ path('video/detail', {'video_id': single_video.getId}) }}">{{ single_video.getUrl }}</a><br>
        <a>ビデオId: {{ single_video.getId }}</a><br>
        <a>登録者Id: {{ single_video.getOwner }}</a>
        <br>
        <br>
        <a href="{{ path('video') }}">
            登録動画一覧画面へ
        </a>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
